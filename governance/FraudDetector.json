{
  "model_id": "fraud-detector",
  "model_name": "FraudDetector",
  "owner": "TBD — assign accountable model owner (default: Data Engineering, fraud)",
  "algorithm": "scikit-learn classifier (see airflow/jobs/train_fraud_model.py)",
  "training_period": "synthetic dataset, no calendar period (generate_dummy_fraud_data.py)",
  "training_data": "synthetic fraud dataset (generate_dummy_fraud_data.py); local fallback data/model.pkl",
  "target": "is_fraud (rule-derived synthetic label)",
  "target_type": "synthetic_formula",
  "features": "see governance/data_dictionary.json (fraud feature set documented separately when the fraud pipeline is reviewed)",
  "known_limitations": [
    "is_fraud is generated by a rule (low savings + high requested amount) plus a 2% random flip, not confirmed fraud outcomes.",
    "Served via a 3-tier fallback (served endpoint -> MLflow registry -> local model.pkl); the local fallback can silently serve a stale model.",
    "Label encoders load from disk and return {} if missing, degrading silently."
  ],
  "validation": {
    "method": "not documented in the training job",
    "metric": "not_recorded_here",
    "value": "not_recorded_here"
  },
  "approval_status": "NOT_APPROVED_FOR_PRODUCTION",
  "notes": "Fraud model governance is a first pass; a full review is tracked separately from the credit models."
}
