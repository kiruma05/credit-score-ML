"""Model-governance artifacts: per-model records and the feature data dictionary.

These JSON files are the machine-readable governance layer required for a
bank-grade model risk function — model ownership, target definition, known
limitations, approval status, and a data dictionary that classifies every
feature as sample-point (known at application) vs outcome-point (known later).
"""
